/* Assign jsonObject with a JSON object with properties: name (a string),
 age (a number) and siblings (an array of strings).
  Note: The content of the properties doesn't matter. */

 var jsonObject;
 /* Your solution goes here */
 jsonObject = {
   name: "Cathy",
   age: 20,
   siblings: ["Mark", "Billy"]
 };
